LENS: state and systems. Focus on state transitions, partial failures,
ordering, and duplication — an action applied twice, an operation that left the
world inconsistent, or a read that reflected a state that no longer holds.
